#include "main.h"
#include <stdio.h>
/**
 * print_last_digit - prints the
 * last digit of a number
 * @n: the number
 *
 * Return: Always 0 (success)
 */

int print_last_digit(int n)
{
	return (n % 10);
}
