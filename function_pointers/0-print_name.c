#include <stdio.h>
#include "main.h"

/**
 * print_name - function that
 * prints a name
 * @name: pointer to a name
 * void (*f)(char*))
 * Return: void
 */

void print_name(char *name, void(*f)(char *));
{
	ptr = f;

	name = ptr();
}
