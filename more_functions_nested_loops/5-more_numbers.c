#include <stdio.h>
#include "main.h"

/**
 * more_numbers - check the code
 * Return: Always 0
 */

void more_numbers(void)
{
	int a, b;

	for (a = 0; a <= 14; a++)
	{
		b = 10;
		{
			_putchar((a + '0') * b);
			{
				while (a = 14)
				{
					_putchar('\n');
				}
			}
		}

	}
}
