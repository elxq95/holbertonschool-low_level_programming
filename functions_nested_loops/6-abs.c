#include "main.h"
#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>
/**
 * _abs - check the code
 * @int: check the code
 * Return: 0 (Success)
 */
int _abs(int)
{
	int n;

	if (abs(n) > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
